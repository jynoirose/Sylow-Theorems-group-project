import Mathlib.Tactic
import Mathlib.Data.Set.Card.Arithmetic

import Mathlib.GroupTheory.GroupAction.Defs
import Mathlib.Algebra.Group.Subgroup.Finite
import Mathlib.Data.Fintype.Defs
import Mathlib.Data.Fintype.Card
import Mathlib.GroupTheory.Index
import Mathlib.Data.Fintype.OfMap
import Mathlib.GroupTheory.Coset.Defs
import Mathlib.GroupTheory.GroupAction.ConjAct
import Mathlib.Algebra.Group.Subgroup.Pointwise
import OrbitStabiliser02

lemma claim23 {G : Type*} [Group G] {p n r : ℕ} [Fintype G] [MulAction G (X G p n)]
 [DecidableEq (X G p n)]
    (P : X G p n)
    (hsame : (stabilizer G (P) : Set G) = (P).val)
    (hS : Fintype (stabilizer G (P))) :
    ∀ (P : X G p n), ¬ (p ∣ Fintype.card (orbit G P)) := by
    have h₀ : Fintype.card G = Fintype.card (orbit G P) * Fintype.card (stabilizer G P) := by
      apply orbit_stabilizer_theorem G (X G p n) P
      sorry
    sorry
